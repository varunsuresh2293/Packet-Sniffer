#Packet capturing tool in python: 

A tool identical to Cisco packet tracer, which was developed in Python from scratch. 
Features included packet sniffer (read incoming & outgoing Packet) like TCP, UDP, HTTP, ICMP & ARP packets. 
Additional features included calculate throughput, Diameter of network, congestion window size and plot graphs 
periodically. It was implemented down to the Network Layer.

